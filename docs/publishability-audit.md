# PLC Copilot Publishability Audit

This file is generated by `pnpm publish:audit`. Do not edit by hand —
any drift fails CI via `pnpm publish:audit --check`.

## Summary

| Metric | Count |
| --- | ---: |
| Packages | 9 |
| Publishable candidates | 6 |
| Internal packages | 2 |
| Apps | 1 |
| Blockers | 0 |
| Warnings | 0 |
| Infos | 13 |

## Recommended publish build order

1. `@plccopilot/pir`
2. `@plccopilot/codegen-core`
3. `@plccopilot/codegen-codesys`
4. `@plccopilot/codegen-rockwell`
5. `@plccopilot/codegen-siemens`
6. `@plccopilot/cli`

## Package matrix

| Package | Intent | Private | Exports | Files | Dist JS | Types | Workspace deps | Blockers | Warnings |
| --- | --- | :---: | :---: | :---: | :---: | :---: | ---: | ---: | ---: |
| `@plccopilot/cli` | publishable | no | yes | yes | yes | yes | 5 | 0 | 0 |
| `@plccopilot/codegen-codesys` | publishable | no | yes | yes | yes | yes | 2 | 0 | 0 |
| `@plccopilot/codegen-core` | publishable | no | yes | yes | yes | yes | 1 | 0 | 0 |
| `@plccopilot/codegen-integration-tests` | internal | yes | no | no | no | no | 5 | 0 | 0 |
| `@plccopilot/codegen-rockwell` | publishable | no | yes | yes | yes | yes | 2 | 0 | 0 |
| `@plccopilot/codegen-siemens` | publishable | no | yes | yes | yes | yes | 4 | 0 | 0 |
| `@plccopilot/electrical-ingest` | internal | yes | no | no | no | no | 1 | 0 | 0 |
| `@plccopilot/pir` | publishable | no | yes | yes | yes | yes | 0 | 0 | 0 |
| `@plccopilot/web` | app | yes | no | no | no | no | 6 | 0 | 0 |

## Findings by package

### `@plccopilot/cli` (`packages/cli`)

Intent: **publishable**.
Internal deps: `@plccopilot/codegen-codesys`, `@plccopilot/codegen-core`, `@plccopilot/codegen-rockwell`, `@plccopilot/codegen-siemens`, `@plccopilot/pir`.

#### Infos

- **HAS_BIN** — package declares bin entries: plccopilot
- **HAS_SCHEMA_EXPORTS** — package declares 4 schema subpath exports
- **INTERNAL_DEP** — depends on internal packages: @plccopilot/codegen-codesys, @plccopilot/codegen-core, @plccopilot/codegen-rockwell, @plccopilot/codegen-siemens, @plccopilot/pir

### `@plccopilot/codegen-codesys` (`packages/codegen-codesys`)

Intent: **publishable**.
Internal deps: `@plccopilot/codegen-core`, `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/codegen-core, @plccopilot/pir

### `@plccopilot/codegen-core` (`packages/codegen-core`)

Intent: **publishable**.
Internal deps: `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/pir

### `@plccopilot/codegen-integration-tests` (`packages/codegen-integration-tests`)

Intent: **internal**.
Internal deps: `@plccopilot/codegen-codesys`, `@plccopilot/codegen-core`, `@plccopilot/codegen-rockwell`, `@plccopilot/codegen-siemens`, `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/codegen-codesys, @plccopilot/codegen-core, @plccopilot/codegen-rockwell, @plccopilot/codegen-siemens, @plccopilot/pir
- **INTEGRATION_TESTS_HARNESS** — cross-backend integration test harness; no npm publish surface.

### `@plccopilot/codegen-rockwell` (`packages/codegen-rockwell`)

Intent: **publishable**.
Internal deps: `@plccopilot/codegen-core`, `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/codegen-core, @plccopilot/pir

### `@plccopilot/codegen-siemens` (`packages/codegen-siemens`)

Intent: **publishable**.
Internal deps: `@plccopilot/codegen-codesys`, `@plccopilot/codegen-core`, `@plccopilot/codegen-rockwell`, `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/codegen-codesys, @plccopilot/codegen-core, @plccopilot/codegen-rockwell, @plccopilot/pir

### `@plccopilot/electrical-ingest` (`packages/electrical-ingest`)

Intent: **internal**.
Internal deps: `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/pir
- **INTERNAL_PRIVATE** — internal package with no exports / bin; not intended for publish.

### `@plccopilot/pir` (`packages/pir`)

Intent: **publishable**.

### `@plccopilot/web` (`packages/web`)

Intent: **app**.
Internal deps: `@plccopilot/codegen-codesys`, `@plccopilot/codegen-core`, `@plccopilot/codegen-rockwell`, `@plccopilot/codegen-siemens`, `@plccopilot/electrical-ingest`, `@plccopilot/pir`.

#### Infos

- **INTERNAL_DEP** — depends on internal packages: @plccopilot/codegen-codesys, @plccopilot/codegen-core, @plccopilot/codegen-rockwell, @plccopilot/codegen-siemens, @plccopilot/electrical-ingest, @plccopilot/pir
- **APP_PRIVATE** — Vite app — built and served, not published as an npm package.

## Interpretation

### Current state

No publish blockers reported. Every publishable candidate has a build, types, and resolvable dependencies.

The `@plccopilot/cli` package additionally has the smoke chain from sprints 53–55 verifying its dist, npm pack manifest, and real tarball runtime — but the runtime today only resolves because the workspace lays down `node_modules/@plccopilot/*` symlinks pointing at sibling sources. A real consumer doing `npm install <tgz>` would still fail until the rest of the graph emits compiled JS.

### Minimum path to a consumer-install smoke

1. **Sprint 57** — add `tsconfig.build.json` + `build` script + `dist/` exports for `@plccopilot/pir` and `@plccopilot/codegen-core`.
2. **Sprint 58** — same treatment for the vendor codegen packages (`codegen-codesys`, `codegen-rockwell`, `codegen-siemens`), in topological order.
3. **Sprint 59** — flip the `exports` / `main` / `types` of every publishable package to dist (with a workspace-friendly conditional export so source-of-truth still works inside the monorepo).
4. **Sprint 60** — drop `private: true` on the publishable candidates, decide how `workspace:*` rewrites at pack time (verify pnpm pack vs. npm pack), and add the consumer-install smoke (`npm install <tgz>` in a clean temp project).

This audit will keep flagging the same blockers until each step lands. `pnpm publish:audit` after each sprint should show the count dropping.

